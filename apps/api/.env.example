# Ejemplo de variables de entorno para @satelite/api (Nest.js)
#
# Este archivo muestra los nombres de variables requeridas por el servicio API.
# Copia los valores a un .env local (no se commitea) y ajusta según tu entorno.

# PostgreSQL local (Docker) publicado en 5433
# Formato: postgresql://<user>:<pass>@<host>:<port>/<database>
DATABASE_URL="postgresql://admin:password123@127.0.0.1:5433/satelite_ar_db"

# Base de datos sombra para Prisma Migrate
SHADOW_DATABASE_URL="postgresql://admin:password123@127.0.0.1:5433/satelite_ar_db_shadow"

# Clave de API de la NASA
# Solo el valor de la clave (sin URL). Obtén una en https://api.nasa.gov
NASA_API_KEY="DEMO_KEY"

# Puerto del servidor Nest.js en desarrollo
PORT=3001

# Token de autenticación para endpoints de administración (AdminGuard)
# No lo expongas en el frontend; úsalo solo en pruebas y admin proxy
ADMIN_API_TOKEN=local-api-token-123
# Nota: Copiar a apps/api/.env y completar valores reales en desarrollo.

# Conexión base de datos (local Docker o Vercel Postgres)
#DATABASE_URL="postgresql://admin:password123@127.0.0.1:5433/satelite_ar_db"
#SHADOW_DATABASE_URL="postgresql://admin:password123@127.0.0.1:5433/satelite_ar_db_shadow"

# Clave/URL para APOD (NASA). En la Parte 2, el Service leerá esta variable.
# Puedes usar DEMO_KEY para pruebas limitadas.
NASA_API_KEY="ASPDx4bjo1UIUdYlASbPHUtQXrNeEAR96RqTJqGp"

# Token de administración para proteger endpoints internos (AdminGuard)
#ADMIN_API_TOKEN="cambia-este-token-en-desarrollo-y-produccion"