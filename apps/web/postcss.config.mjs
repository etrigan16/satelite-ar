// PostCSS config para Tailwind CSS v3 + Autoprefixer
// Referencia: https://tailwindcss.com/docs/installation
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
